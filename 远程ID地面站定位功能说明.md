# 远程 ID 地面站定位功能说明

## 功能概述

本次修改优化了 Mission Planner 远程 ID（Remote ID）设置界面中的地面站定位功能，将相关控件整合到"地面站定位"区域，提升了用户体验和功能的逻辑性。

## 主要修改内容

### 1. "使用起飞点作为飞手位置"功能

**位置调整：**
- 该复选框已从主界面移动到"地面站定位"（NMEA_GPS_Connection）控件的 GroupBox 内部
- 新位置：位于"地面站定位"选项框区域的底部，状态显示标签下方

**功能说明：**
- 当该选项被勾选时，系统会自动将无人机的 Home 点（起飞位置）坐标直接映射为飞手（地面站）的实时地理位置
- 适用场景：飞手基本保持在起飞位置、不发生明显移动的场景
- 优势：省去了额外连接地面站 GPS 硬件的需求

**交互逻辑：**
- 勾选该选项后，GPS 连接相关的控件（串口选择、波特率、连接按钮、自动连接）将自动禁用
- 取消勾选后，这些控件将重新启用，允许用户连接外部 GPS 设备

### 2. "手动输入坐标"功能

**触发方式：**
- 在"地面站定位"区域的状态显示标签上点击鼠标右键
- 弹出右键菜单，选择"手动输入坐标..."选项

**功能说明：**
- 适用场景：当前环境没有 GPS 信号，也无法进行硬件定位时
- 允许用户手动输入具体的经度、纬度和高度数值
- 即使在室内测试或无信号区域，也可以通过人工干预的方式为地面站强制指定一个虚拟坐标

**输入对话框：**
- 纬度：范围 -90° 到 90°
- 经度：范围 -180° 到 180°
- 高度：单位为米（m）
- 输入格式：支持小数点格式，例如 39.123456

**数据验证：**
- 系统会自动验证输入的经纬度是否在有效范围内
- 如果输入格式错误或超出范围，会显示相应的错误提示

**状态显示：**
- 手动输入坐标后，状态标签会显示"手动输入: [纬度], [经度], [高度] m"
- 系统会将手动输入的坐标用于远程 ID 信息上报

## 技术实现细节

### 修改的文件

1. **Plugins/OpenDroneID2/NMEA_GPS_Connection.Designer.cs**
   - 添加了 `CHK_use_home_operator_location` 复选框控件
   - 添加了右键菜单 `contextMenuStrip1` 和菜单项 `menuItem_ManualInput`
   - 调整了 GroupBox 的高度以容纳新增的复选框
   - 为状态标签添加了右键菜单绑定

2. **Plugins/OpenDroneID2/NMEA_GPS_Connection.cs**
   - 在 `PointNMEA` 类中添加了 `IsManualOverride` 属性，用于标识手动输入的坐标
   - 实现了 `CHK_use_home_operator_location_CheckedChanged` 事件处理
   - 实现了 `UpdateControlsState` 方法，用于根据复选框状态启用/禁用相关控件
   - 实现了 `menuItem_ManualInput_Click` 方法，显示手动输入对话框并处理用户输入
   - 在构造函数中初始化复选框状态

3. **Plugins/OpenDroneID2/OpenDroneID_UI.Designer.cs**
   - 移除了 `CHK_use_home_operator_location` 复选框的定义和初始化代码

4. **Plugins/OpenDroneID2/OpenDroneID_UI.cs**
   - 移除了复选框相关的事件处理代码
   - 保持了对 `Settings.Instance["ODID_UseHomeOperatorLocation"]` 的读取逻辑（在 OpenDroneID_UI.cs 的 checkGCSGPS 方法中）

## 用户使用指南

### 使用起飞点作为飞手位置

1. 打开 Mission Planner 的"远程 ID"页面
2. 在"地面站定位"区域找到"使用起飞点作为飞手位置"复选框
3. 勾选该选项
4. 系统将自动使用无人机的 Home 点作为飞手位置

### 手动输入坐标

1. 在"地面站定位"区域的状态显示标签上点击鼠标右键
2. 选择"手动输入坐标..."
3. 在弹出的对话框中输入纬度、经度和高度
4. 点击"确定"保存
5. 状态标签将显示手动输入的坐标信息

## 注意事项

1. "使用起飞点作为飞手位置"和"连接外部 GPS"是互斥的功能，勾选前者会自动禁用后者的相关控件
2. 手动输入的坐标仅在当前会话有效，重启软件后需要重新输入
3. 手动输入坐标时，请确保输入的数值准确，因为这会影响远程 ID 的上报信息
4. 建议在正式飞行时使用真实的 GPS 定位，手动输入功能主要用于测试和特殊场景

## 兼容性

- 该修改保持了与现有设置系统的兼容性
- 设置项 `ODID_UseHomeOperatorLocation` 会自动保存到配置文件中
- 升级后，用户之前的设置会被保留
